6 analizis de nmap 
1 script y 1 archivo de texto con los resultados codificados en base 64
obtener tu ip local y publica: *ip address
-hostname -I
-curl ifconfig.me
map a: *su segmento de red privado una ip desu segmento (o scanme.nmap.org) con un script
*su ip publica